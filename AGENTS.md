# Codex instructions

Before working in this repository, read and follow `CLAUDE.md` at the repository root. When working inside a submodule or directory that has its own `CLAUDE.md`, read and follow that file too.

`CLAUDE.md` files are the single source of truth for project instructions. Do not duplicate their contents here.
